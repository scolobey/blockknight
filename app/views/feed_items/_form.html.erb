<%= form_with(model: feed_item, local: true) do |form| %>
  <% if feed_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feed_item.errors.count, "error") %> prohibited this feed_item from being saved:</h2>

      <ul>
      <% feed_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :feed_item_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :feed_item_description %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_area :image, id: :feed_item_image %>
  </div>

  <div class="field">
    <%= form.label :approved %>
    <%= form.check_box :approved, id: :feed_item_approved %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :feed_item_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
